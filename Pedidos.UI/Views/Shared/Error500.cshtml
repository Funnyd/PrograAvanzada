@{
    ViewBag.Title = "Error del Servidor";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row tm-mt-big">
    <div class="col-12 text-center">
        <div class="bg-white tm-block py-5">
            <i class="fas fa-server fa-5x text-danger mb-4"></i>
            <h2 class="tm-block-title">Error 500 - Error del Servidor</h2>
            <p class="text-muted">Ha ocurrido un error interno en el servidor.</p>
            <a href="@Url.Action("Index", "Home")" class="btn btn-primary mt-3">
                <i class="fas fa-home"></i> Volver al Dashboard
            </a>
        </div>
    </div>
</div>